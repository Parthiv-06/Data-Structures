def word_reversing(s):
    words=s.split()
    res=[]
    for i in range(len(words)-1,-1,-1):
        res.append(words[i])
        if i!=0:
            res.append(" ")
    return "".join(res)
s="you are so beautiful"
print(word_reversing(s))
